dbt_wiki_analytics:
  outputs:
    dev:
      type: duckdb
      path: ../data/wikipedia.db
      schema: iv         # Specified schema 'iv'
      threads: 1
      extensions:        # may read from parquet, generated by API call.
        - parquet
  target: dev
